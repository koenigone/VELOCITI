# production env: Vite reads this during "vite build"
# VITE_VELOCITI_API_KEY is set in Render dashboard (baked into the bundle at build time)
# Socket URL left empty - socket.io-client auto-connects to same origin in production
VITE_VELOCITI_SOCKET_URL=